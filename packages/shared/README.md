# Shared

Shared TypeScript package placeholder for cross-service contracts that are safe to reuse between the dashboard and TypeScript services.

Do not place `rag-api` implementation internals here. Keep service boundaries explicit.
